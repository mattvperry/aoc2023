import { readInputLines } from '../shared/utils';

const day1 = (lines: string[]): number => {
    return 0;
}

(async () => {
    const input = await readInputLines('day1');
    const part1 = day1(input);

    console.log(part1);
})();
